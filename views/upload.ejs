<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>图片上传</title>
	<style>
        /*上传按钮样式*/
        .uploadBox{
            position: absolute;
            left: 0;
            top: 0;
        }
        .upload,.uploadToServer{
            position: absolute;
            width: 100px;
            background-color: #9ec9ec;
            text-align: center;
            height: 30px;
            line-height: 30px;
            color: #ffffff;
            font-size: 14px;
            text-decoration: none;
            z-index: 3;
            padding: 0;
            margin: 0;
            border:none;
        }
        .uploadToServer{z-index: 4;
            left: 110px;}
        .uploadBtn{
            position: absolute;
            filter:alpha(opacity=0);
            opacity:0;
            left: 0;
            top: 0;
            z-index: 2;
            width: 100%;
            height: 100%;
        }
    </style>
    <script>
        function uploadFile() {
            var upload = document.getElementById('uploadBtn');
            //Make sure choose a file
            if(upload.files.length) return true;
            return false;
        }
    </script>
</head>
<body>
	<!-- enctype="multipart/form-data" 多功能媒体类型 图片 音乐 flash等-->
	<!-- enctype="application/x-www-form-urlencoded" 这个是默认值 -->
	<!-- enctype="text/plain" 这个是邮件专用 -->
	<form action="/upload" onsubmit="return uploadFile()" enctype="multipart/form-data" method="post">
        <div class="uploadBox">
            <a class="upload" href="javascript:void(0)">
            	选择图片
            	<input class="uploadBtn" id="uploadBtn" type="file" name="uploadBtn"/>
            </a>
            <input class="uploadToServer" name="uploadToServer" id="uploadToServer" type="submit" value="上传">
            <!-- <a class="uploadToServer" id="uploadToServer" href="javascript:void(0)">上传</a> -->
        </div>
    </form>
</body>
</html>